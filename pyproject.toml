[project]
name = "glinet-py"
version = "0.1.0"
description = "GL.iNet router 4.x API client"
requires-python = ">=3.10"
dependencies = [
    "passlib>=1.7",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "jsonschema>=4",
]
